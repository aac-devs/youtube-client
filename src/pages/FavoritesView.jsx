import styled from 'styled-components';

const Container = styled.main`
  padding: 10px;
  margin: 0 auto;
  max-width: 1700px;
  width: 100%;
  padding-top: 74px;
`;

const FavoritesView = () => {
  return <Container>Favorites View</Container>;
};

export default FavoritesView;
